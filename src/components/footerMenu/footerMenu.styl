$launch-height = 30px

.c-box-skin
  height: 50px
  width: 100%
  &--bgColor
    bgColor(white)

.menuTitle
  height: 50px
  .title
    justify-content: center
    font-size: 16px
    &:hover
      color: $color-primary
